<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="menus">
<!-- 
	<insert id="menuClick" parameterType="map" statementType="CALLABLE">
		{CALL sp_usermenu_set(#{id},#{m_name},#{m_link})}
	</insert>	
 -->
 	<select id="menuClick" parameterType="map">
 		SELECT sp_usermenu_set(#{id},#{m_name},#{m_link})
 	</select>
 
	<select id="tabList" resultType="String" parameterType="String">
		SELECT m_name, MAX(regdate) AS regdate
		FROM (
			SELECT CONCAT(m_name,'_',m_link) AS m_name, regdate
			FROM tb_usermenu
			WHERE yn = 'Y'
			AND id= #{value}
		) AS aaa
		GROUP BY m_name
		ORDER BY regdate DESC
	</select>
	
	<select id="menuCount" resultType="String" parameterType="String">
		SELECT m_link
		FROM tb_usermenu
		WHERE id = #{value} AND yn='Y'
		ORDER BY regdate DESC
		LIMIT 1
	</select>
	
	<update id="menuDelete" parameterType="java.util.Map">
		UPDATE tb_usermenu SET yn='N'
		WHERE m_name = #{m_name} AND id = #{id}
	</update>
	
</mapper>